#include "Table.h"
#include <iostream>

using namespace std;

int main(){
    Table t1;
    t1.playGame();
    // return 0;
    system("pause"); // switch to return 0 if you don't use vscode to open external terminal
}